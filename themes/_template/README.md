# Theme Template

Create overrides inside `files/` using the same relative paths listed in `docs/theme-manager.md`.

Only files under supported application paths are read by `themectl`. If a file is omitted, `themes/_base/files` supplies the fallback version.

